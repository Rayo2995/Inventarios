import styled from "styled-components";

export function ActionTabla({ onClick, icono, color, fontSize }) {
    return (
        <Container>
            <h1>Componentex</h1>
        </Container>
    );
}

const Container = styled.div`
    /* estilos aquí */
`;